# Package Owner tools

## set-publish-config.ts

`set-publish-config.ts` sets `publishConfig` on each package.

```
  "publishConfig": {
    "access": "public"
  }
```

This keeps public package publishing metadata consistent.

## update-rule-preset.ts

`update-rule-preset.ts` updates preset package dependencies and source.
